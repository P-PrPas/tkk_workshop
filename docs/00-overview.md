# 00 — ภาพรวมและงบเวลา

## เป้าหมาย
สอน Computer Vision 101 ใน **90 นาที** ด้วย `.ipynb` เดียวบน Google Colab ที่ผู้เรียน
"กดรันเซลล์ตามลำดับแล้วได้ผลลัพธ์" โดยไม่ต้องแก้โค้ด แล้วปิดท้ายด้วย desktop app
ที่วิทยากรรันโชว์ เพื่อสื่อสารสารหลัก:

> **การทำให้ "พอใช้ได้" นั้นง่าย แต่การทำให้ "ใช้งานได้จริง" นั้นยาก**

## สารรอง (ค้นพบระหว่างออกแบบ — สำคัญกว่าที่คิด)
โมเดลจิ๋วของเราเทรนต่อจาก `yolo11n.pt` ที่ผ่าน COCO มาแล้ว ดังนั้นที่มันใช้งานได้
**ไม่ใช่เพราะรูป 10 ใบของเรา แต่เพราะน้ำหนักตั้งต้น** — ต้องพูดตรงๆ หน้าห้อง
นี่คือบทเรียน transfer learning ที่จริงกว่าการแกล้งทำเป็นว่าเราเทรนสำเร็จเอง

## ผู้เรียน
เขียน Python ได้ ไม่เคยทำ CV มาก่อน ไม่ต้องรู้คณิตศาสตร์ของ loss

## งบเวลา 90 นาที
| ช่วง | นาที | สะสม |
|---|---|---|
| เปิด + เล่าโครงเรื่อง | 5 | 5 |
| เซลล์ 0: preflight + `pip install` (pin แล้ว) | 6 | 11 |
| 1.1 โหลด data + ดูรูป/label | 6 | 17 |
| 1.2 เทรน 3 epoch | 6 | 23 |
| 1.3 ผลลัพธ์บน test set | 8 | 31 |
| 1.4 realtime ผ่านกล้อง (รวมเวลาขออนุญาตกล้อง) | 12 | 43 |
| พัก / แก้ปัญหาคนตกขบวน | 7 | 50 |
| 2. hand pose + กติกา กำ/แบ + realtime | 15 | 65 |
| 3. รวมร่าง "คนถือแก้ว" + realtime | 12 | 77 |
| Desktop app โชว์หน้าห้อง + สรุปสาร | 13 | 90 |

**ไม่มีเวลาเหลือ** ทุกเซลล์ที่เพิ่มเข้ามาต้องแลกกับเซลล์อื่นที่ถูกตัด

## สถาปัตยกรรมโดยรวม
```
Colab notebook (ผู้เรียน)          Desktop app (วิทยากร)
─────────────────────────          ─────────────────────
yolo11n 3 epoch / 10 รูป           yolo11s เทรนเต็มจาก Open Images
MediaPipe HandLandmarker            MediaPipe HandLandmarker (เหมือนกัน)
กติกาต่อเฟรม ไม่มีความจำ            ByteTrack + state machine + hysteresis
JS webcam ~3-8 FPS                  threaded capture, FPS budget
พังก็พัง                            กล้องหลุดแล้วต่อใหม่ได้
```

## การตัดสินใจที่ล็อกแล้ว
| หัวข้อ | ผล | เหตุผล |
|---|---|---|
| Runtime | Google Colab | ไม่ต้องติดตั้งอะไรบนเครื่องผู้เรียน |
| Realtime | JS webcam loop → helper `run_webcam()` ตัวเดียว | `cv2.imshow` / `VideoCapture(0)` ใช้ไม่ได้บน Colab |
| Dataset จิ๋ว | 15 รูปพร้อม label YOLO → 10/2/3 | เจ้าของงานเตรียมให้ ไม่มีขั้นตอน label |
| คลาส | 1 class `cup` = แก้วดื่มทุกแบบ ไม่รวมขวด | yaml ง่าย, logic ข้อ 3 ง่าย, ขวดกลายเป็น teaching moment |
| น้ำหนักตั้งต้น | `yolo11n.pt` (COCO) | เริ่มจากศูนย์ = ตรวจไม่เจออะไรเลย = พาร์ท 3 พัง |
| Hand pose | MediaPipe HandLandmarker | Ultralytics ปล่อยแค่ body pose 17 จุด ไม่มี hand checkpoint สำเร็จรูป |
| กติกาถือแก้ว | hand bbox ซ้อน cup bbox **และ** มือกำ | อธิบายเป็นภาษาไทยได้ในประโยคเดียว |
| โมเดลดี | yolo11s เทรนบน V100 จาก Open Images V7 | เล็กพอรันสดบนแล็ปท็อป |
| App | วิทยากรรันคนเดียว, โมเดลดีอย่างเดียว ไม่เทียบในแอป | ตัดปัญหา env ของผู้เรียน 20 เครื่อง |

## ทะเบียนความเสี่ยง
| # | ความเสี่ยง | ผลกระทบ | การรับมือ |
|---|---|---|---|
| R1 | browser ผู้เรียนบล็อกกล้อง | 1.4 / 2 / 3 ตายกลางห้อง | บอกให้กด Allow / ย้ายไป Chrome ก่อนเริ่ม; ยังไม่ได้ให้วิทยากรอัดคลิปสดแล้วรัน `run_video("clip.mp4", ...)` แทน |
| R2 | Colab เปลี่ยนเวอร์ชัน Python/lib | ติดตั้งพัง | pin ทุกเวอร์ชัน + เซลล์ preflight ที่ assert แล้วพังดังๆ ตั้งแต่นาทีแรก |
| R3 | Wi-Fi ห้องช้า ตอนโหลด weight/dataset | ทุกคนค้างพร้อมกัน | dataset ~5MB, `yolo11n.pt` ~6MB; บอกผู้เรียนรันเซลล์ 0-1 ตั้งแต่ก่อนเริ่ม |
| R4 | val 2 รูป ทำให้ mAP ไร้ความหมาย | ผู้เรียนเชื่อตัวเลขผิดๆ | โชว์กริดรูปเป็นหลักฐานหลัก + พูดตรงๆ ว่าตัวเลขนี้เชื่อไม่ได้ |
| R5 | เทรนโมเดลดีไม่ทันวันงาน | ไม่มี app โชว์ | fallback: ใช้ `yolo11m.pt` COCO ตรงๆ (class 41 = cup) แทน best.pt |
| R6 | 3-8 FPS บน Colab ทำให้ผู้เรียนคิดว่า CV ช้า | สารเพี้ยน | พูดล่วงหน้าว่าคอขวดคือ browser ไม่ใช่โมเดล แล้วให้ app พิสูจน์ |

## Runbook วันงาน
**ก่อนงาน 1 วัน**
- [ ] รันโน้ตบุ๊กจนจบจาก runtime เปล่า จับเวลาจริง
- [ ] ทดสอบ app บนแล็ปท็อปที่จะใช้ + กล้องตัวที่จะใช้ + แสงในห้องจริง
- [ ] เตรียมแก้วน้ำจริงวางบนโต๊ะ อย่างน้อย 2 ใบต่างแบบ
- [ ] ดาวน์โหลด weight ทั้งหมดไว้ในเครื่องแล้ว (อย่าพึ่ง Wi-Fi ห้อง)

**ก่อนเริ่ม 15 นาที**
- [ ] ให้ผู้เรียนเปิดโน้ตบุ๊กและกดรันเซลล์ 0 ทันทีที่เข้าห้อง
- [ ] เช็กว่ามีคนใช้ Safari หรือไม่ (getUserMedia งอแงที่สุด) → แนะนำให้ย้ายไป Chrome

**ถ้าพัง**
- กล้องไม่ติด → กด Allow ที่ address bar / ย้ายไป Chrome / รันเซลล์ใหม่; สุดทางคืออัดคลิปสดแล้ว `run_video("clip.mp4", ...)`
- Colab หลุด → runtime ใหม่ต้องรันเซลล์ 0-1 ใหม่ (~3 นาที) มีเซลล์ resume ให้
- ตรวจไม่เจอแก้ว → ลด `conf` เหลือ 0.15 (บอกไว้ในโน้ตบุ๊กแล้ว)

## เอกสารอื่น
- [01-notebook-spec.md](01-notebook-spec.md) — สเปกรายเซลล์
- [02-data.md](02-data.md) — คู่มือถ่ายรูป, auto-label, โครง data repo
- [03-models.md](03-models.md) — คอนฟิกเทรนทั้งสองโมเดล
- [04-desktop-app.md](04-desktop-app.md) — สถาปัตยกรรมแอป
